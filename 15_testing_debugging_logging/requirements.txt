# Testing, debugging, and logging dependencies.
#
# Install into your active virtual environment:
#   pip install -r requirements.txt
#
# `pdb` and `logging` are part of the standard library — no install needed.

pytest>=8.0
pytest-cov>=5.0
