# lists example
# create a list of numbers
numbers = [1, 2, 3, 4, 5]
# print the list
print(numbers)
# access the first element
print(numbers[0])
# access the last elementprint(numbers[-1])
# add a new element to the end of the list
numbers.append(6)
print(numbers)
# insert a new element at the beginning of the list
numbers.insert(0, 0)
print(numbers)
# remove the last element from the list
numbers.pop()
print(numbers)
# remove the first element from the list
numbers.pop(0)
print(numbers)
# get the length of the listprint(len(numbers))
# iterate over the list and print each element
for number in numbers:
    print(number)
# create a list of strings
fruits = ["apple", "banana", "cherry"]
# print the list
print(fruits)
# access the first element
print(fruits[0])
# access the last element
print(fruits[-1])
# add a new element to the end of the list
fruits.append("date")
print(fruits)
# insert a new element at the beginning of the list
fruits.insert(0, "avocado")
print(fruits)
# remove the last element from the list
fruits.pop()
print(fruits)
# remove the first element from the list
fruits.pop(0)
print(fruits)
# get the length of the listprint(len(fruits))
# iterate over the list and print each element
for fruit in fruits:
    print(fruit)
