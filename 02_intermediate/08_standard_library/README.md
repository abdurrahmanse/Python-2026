# 08 — Standard Library

Tour of the standard library you'll use daily: file paths, dates, randomness, math.

## Subfolders

1. `01_os_pathlib/` — `os` and `pathlib` for filesystem work
2. `02_datetime_random_math/` — `datetime`, `random`, `math`

## Run them in order

```bash
for d in 0?_*/; do python3 "$d/app.py"; done
```

## Next

You've finished the intermediate level. Move on to [`01_testing`](../../03_advanced/01_testing) in the advanced level.